#include <stdlib.h>
#include <stdio.h>

int
main(void) {

	if ( 0 ) {
		printf ( "0 is true\n" );
	} else {
		printf ( "0 is false\n" );
	}

	if ( 1 ) {
		printf ( "1 is true\n" );
	} else {
		printf ( "1 is false\n" );
	}

	exit(0);
}
